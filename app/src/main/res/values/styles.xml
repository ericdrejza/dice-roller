<resources>

    <!-- Base application theme. -->
    <style name="Theme.DiceRoller" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="die_button" parent="TextAppearance.MaterialComponents.Button">
        <item name="backgroundTint">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:layout_marginHorizontal">24dp</item>
        <item name="rippleColor">@color/cyan</item>
        <item name="itemRippleColor">@color/green</item>
        <item name="android:colorActivatedHighlight">@color/purple</item>
        <item name="android:colorPressedHighlight">@color/red</item>

    </style>

</resources>
